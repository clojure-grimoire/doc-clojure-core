{:added "1.9", :ns #object[clojure.lang.Namespace 0x264888e7 "clojure.core"], :name "bounded-count", :file "clojure/core.clj", :type :fn, :src "(defn bounded-count\n  \"If coll is counted? returns its count, else will count at most the first n\n  elements of coll using its seq\"\n  {:added \"1.9\"}\n  [n coll]\n  (if (counted? coll)\n    (count coll)\n    (loop [i 0 s (seq coll)]\n      (if (and s (< i n))\n        (recur (inc i) (next s))\n        i))))", :column 1, :line 7304, :arglists ([n coll]), :doc "If coll is counted? returns its count, else will count at most the first n\n  elements of coll using its seq", :namespace "clojure.core"}