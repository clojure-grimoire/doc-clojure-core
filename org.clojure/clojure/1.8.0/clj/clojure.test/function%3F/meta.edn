{:added "1.1", :ns "clojure.test", :name "function?", :file "clojure/test.clj", :type :fn, :src "(defn function?\n  \"Returns true if argument is a function or a symbol that resolves to\n  a function (not a macro).\"\n  {:added \"1.1\"}\n  [x]\n  (if (symbol? x)\n    (when-let [v (resolve x)]\n      (when-let [value (get-possibly-unbound-var v)]\n        (and (fn? value)\n             (not (:macro (meta v))))))\n    (fn? x)))", :column 1, :line 423, :arglists ([x]), :doc "Returns true if argument is a function or a symbol that resolves to\n  a function (not a macro)."}