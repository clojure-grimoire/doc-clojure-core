{:added "1.1", :ns "clojure.walk", :name "keywordize-keys", :file "clojure/walk.clj", :type :fn, :src "(defn keywordize-keys\n  \"Recursively transforms all map keys from strings to keywords.\"\n  {:added \"1.1\"}\n  [m]\n  (let [f (fn [[k v]] (if (string? k) [(keyword k) v] [k v]))]\n    ;; only apply to maps\n    (postwalk (fn [x] (if (map? x) (into {} (map f x)) x)) m)))", :column 1, :line 93, :arglists ([m]), :doc "Recursively transforms all map keys from strings to keywords."}