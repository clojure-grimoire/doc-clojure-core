{:type :fn, :src "(defmulti use-fixtures\n  \"Wrap test runs in a fixture function to perform setup and\n  teardown. Using a fixture-type of :each wraps every test\n  individually, while:once wraps the whole run in a single function.\"\n  {:added \"1.1\"}\n  (fn [fixture-type & args] fixture-type))", :ns "clojure.test", :name "use-fixtures", :added "1.1", :file "clojure/test.clj", :column 1, :line 657, :doc "Wrap test runs in a fixture function to perform setup and\n  teardown. Using a fixture-type of :each wraps every test\n  individually, while:once wraps the whole run in a single function."}