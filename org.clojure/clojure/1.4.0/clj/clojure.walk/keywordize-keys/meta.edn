{:type :fn, :src "(defn keywordize-keys\n  \"Recursively transforms all map keys from strings to keywords.\"\n  {:added \"1.1\"}\n  [m]\n  (let [f (fn [[k v]] (if (string? k) [(keyword k) v] [k v]))]\n    ;; only apply to maps\n    (postwalk (fn [x] (if (map? x) (into {} (map f x)) x)) m)))", :ns "clojure.walk", :name "keywordize-keys", :arglists ([m]), :added "1.1", :doc "Recursively transforms all map keys from strings to keywords.", :line 91, :file "clojure/walk.clj"}