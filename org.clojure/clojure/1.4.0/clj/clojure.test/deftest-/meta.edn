{:arglists ([name & body]), :ns "clojure.test", :name "deftest-", :src "(defmacro deftest-\n  \"Like deftest but creates a private var.\"\n  {:added \"1.1\"}\n  [name & body]\n  (when *load-tests*\n    `(def ~(vary-meta name assoc :test `(fn [] ~@body) :private true)\n          (fn [] (test-var (var ~name))))))", :added "1.1", :type :macro, :doc "Like deftest but creates a private var.", :macro true, :line 623, :file "clojure/test.clj"}