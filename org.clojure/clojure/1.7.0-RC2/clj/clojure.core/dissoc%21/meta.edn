{:added "1.1", :ns "clojure.core", :name "dissoc!", :file "clojure/core.clj", :static true, :type :fn, :src "(defn dissoc!\n  \"Returns a transient map that doesn't contain a mapping for key(s).\"\n  {:added \"1.1\"\n   :static true}\n  ([^clojure.lang.ITransientMap map key] (.without map key))\n  ([^clojure.lang.ITransientMap map key & ks]\n   (let [ret (.without map key)]\n     (if ks\n       (recur ret (first ks) (next ks))\n       ret))))", :column 1, :line 3248, :arglists ([map key] [map key & ks]), :doc "Returns a transient map that doesn't contain a mapping for key(s)."}