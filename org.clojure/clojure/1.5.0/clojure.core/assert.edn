{:arglists ([x] [x message]), :ns "clojure.core", :name "assert", :src "(defmacro assert\n  \"Evaluates expr and throws an exception if it does not evaluate to\n  logical true.\"\n  {:added \"1.0\"}\n  ([x]\n     (when *assert*\n       `(when-not ~x\n          (throw (new AssertionError (str \"Assert failed: \" (pr-str '~x)))))))\n  ([x message]\n     (when *assert*\n       `(when-not ~x\n          (throw (new AssertionError (str \"Assert failed: \" ~message \"\\n\" (pr-str '~x))))))))", :column 1, :added "1.0", :type :macro, :doc "Evaluates expr and throws an exception if it does not evaluate to\n  logical true.", :macro true, :line 4346, :file "clojure/core.clj"}