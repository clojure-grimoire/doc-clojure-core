{:arglists ([test then] [test then else]), :ns "clojure.core", :name "if-not", :src "(defmacro if-not\n  \"Evaluates test. If logical false, evaluates and returns then expr, \n  otherwise else expr, if supplied, else nil.\"\n  {:added \"1.0\"}\n  ([test then] `(if-not ~test ~then nil))\n  ([test then else]\n   `(if (not ~test) ~then ~else)))", :column 1, :added "1.0", :type :macro, :doc "Evaluates test. If logical false, evaluates and returns then expr, \n  otherwise else expr, if supplied, else nil.", :macro true, :line 719, :file "clojure/core.clj"}