{:arglists ([map kmap]), :ns "clojure.set", :name "rename-keys", :src "(defn rename-keys\n  \"Returns the map with the keys in kmap renamed to the vals in kmap\"\n  {:added \"1.0\"}\n  [map kmap]\n    (reduce \n     (fn [m [old new]]\n       (if (contains? map old)\n         (assoc m new (get map old))\n         m)) \n     (apply dissoc map (keys kmap)) kmap))", :column 1, :added "1.0", :type :fn, :doc "Returns the map with the keys in kmap renamed to the vals in kmap", :line 77, :file "clojure/set.clj"}