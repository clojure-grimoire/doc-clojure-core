{:arglists ([keys vals]), :ns "clojure.core", :name "zipmap", :src "(defn zipmap\n  \"Returns a map with the keys mapped to the corresponding vals.\"\n  {:added \"1.0\"\n   :static true}\n  [keys vals]\n    (loop [map {}\n           ks (seq keys)\n           vs (seq vals)]\n      (if (and ks vs)\n        (recur (assoc map (first ks) (first vs))\n               (next ks)\n               (next vs))\n        map)))", :column 1, :added "1.0", :static true, :type :fn, :doc "Returns a map with the keys mapped to the corresponding vals.", :line 2706, :file "clojure/core.clj"}