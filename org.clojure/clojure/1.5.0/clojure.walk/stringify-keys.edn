{:arglists ([m]), :ns "clojure.walk", :name "stringify-keys", :src "(defn stringify-keys\n  \"Recursively transforms all map keys from keywords to strings.\"\n  {:added \"1.1\"}\n  [m]\n  (let [f (fn [[k v]] (if (keyword? k) [(name k) v] [k v]))]\n    ;; only apply to maps\n    (postwalk (fn [x] (if (map? x) (into {} (map f x)) x)) m)))", :column 1, :added "1.1", :type :fn, :doc "Recursively transforms all map keys from keywords to strings.", :line 99, :file "clojure/walk.clj"}